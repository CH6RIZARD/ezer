{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile=false && pnpm --filter @ezer/db exec prisma generate && pnpm --filter @ezer/db build && pnpm --filter @ezer/shared build && pnpm --filter @ezer/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @ezer/api start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
